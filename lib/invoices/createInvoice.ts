export function createInvoice(input:{businessName:string;customerName:string;jobTitle:string;price:number;currency?:string}) { return { invoiceNumber:`TR-${Date.now()}`, customerName:input.customerName, lineItems:[{description:input.jobTitle,amount:input.price}], subtotal:input.price, taxAmount:0, total:input.price, currency:input.currency ?? 'GBP', status:'draft' as const, paymentLink: process.env.STRIPE_SECRET_KEY ? undefined : `https://pay.traden.ai/mock/${Date.now()}` }; }
