import { Cards } from '@/components/AppPages';
export default function Page(){return <Cards title="Leads CRM" items={['new','qualifying','quoted','follow_up_due','booking_pending','booked','completed','payment_pending','paid','review_requested','lost']}/>}
