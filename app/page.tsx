import Link from 'next/link';
const features=['AI lead replies','Quote from your own prices','WhatsApp/SMS/email inbox','Google Calendar booking','Follow-ups','Invoice/payment/review flow'];
export default function Home(){return <main className="shell"><section style={{padding:'70px 0'}}><p className="pill">Demo mode ready • real integrations when connected</p><h1 style={{fontSize:'clamp(44px,7vw,88px)',lineHeight:'.95',maxWidth:900}}>Turn trade enquiries into booked jobs.</h1><p className="muted" style={{fontSize:22,maxWidth:760}}>Traden replies to leads, creates quotes, books jobs, tracks your pipeline, sends invoices and gets reviews, so you can stay on the tools.</p><div style={{display:'flex',gap:12,marginTop:28}}><Link className="btn" href="/signup">Start free demo</Link><Link className="btn btn-secondary" href="/test-lab">Book a demo</Link></div></section><section className="grid-auto">{features.map(f=><div className="card" key={f}><h3>{f}</h3><p className="muted">Built for fast decisions, owner approval and practical trade workflows.</p></div>)}</section><section className="card" style={{marginTop:22}}><h2>Built for mobile trades</h2><div className="nav">{['Electricians','Gas engineers','Roofers','Mobile detailers','Cleaners','Plumbers','Landscapers','Window tinters'].map(x=><span className="pill" key={x}>{x}</span>)}</div></section><section className="grid-auto" style={{marginTop:22}}><div className="card"><h2>Demo workflow</h2><ol className="muted"><li>Customer message arrives.</li><li>Traden qualifies it and prices from rules.</li><li>Owner approves the action card.</li><li>Job booked, invoiced and reviewed.</li></ol></div><div className="card"><h2>Pricing teaser</h2><p className="muted">Start with a free demo. Connect WhatsApp, SMS, Gmail, Calendar and Stripe when ready.</p><Link className="btn" href="/test-lab">Try the lead test lab</Link></div></section></main>}
