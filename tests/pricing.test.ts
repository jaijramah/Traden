import { describe, expect, it } from 'vitest';
import { priceLead } from '@/lib/pricing/priceLead';
const business = { defaultCurrency:'GBP' }; const service = { pricingStrategy:'fixed_price', requiredFields:['postcode'], autoQuoteAllowed:true };
describe('pricing engine', () => { it('fixed price exact match', () => { const r = priceLead({ business, service, priceRules:[{pricingStrategy:'fixed_price',basePrice:85,currency:'GBP'}], jobVariables:{postcode:'ST16'} }); expect(r.price).toBe(85); expect(r.canQuote).toBe(true); }); it('package modifier', () => { const r = priceLead({ business, service:{...service,pricingStrategy:'package_price',requiredFields:['vehicle_size']}, priceRules:[{pricingStrategy:'package_price',basePrice:140,modifiers:[{field:'vehicle_size',equals:'large',add:30}]}], jobVariables:{vehicle_size:'large'} }); expect(r.price).toBe(170); }); it('distance owner_required', () => { const r = priceLead({ business, service, priceRules:[{pricingStrategy:'fixed_price',basePrice:85,modifiers:[{field:'distance_miles',gt:25,action:'owner_required'}]}], jobVariables:{postcode:'ST16'}, distanceMiles:30 }); expect(r.requiresOwnerApproval).toBe(true); expect(r.canQuote).toBe(false); }); it('quote_after_photos blocks auto quote', () => { const r = priceLead({ business, service:{...service,pricingStrategy:'quote_after_photos'}, priceRules:[{pricingStrategy:'quote_after_photos'}], jobVariables:{postcode:'ST16'} }); expect(r.canQuote).toBe(false); expect(r.missingFields).toContain('photos'); }); it('urgency modifier', () => { const r = priceLead({ business, service, priceRules:[{pricingStrategy:'fixed_price',basePrice:90,modifiers:[{field:'urgency',equals:'emergency',add:50}]}], jobVariables:{postcode:'ST16'}, urgency:'emergency' }); expect(r.price).toBe(140); }); });
